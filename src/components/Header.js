const Header = (props) => {

    return (
        <div className="navBar">
            <nav className="nav">
                <h1>Nav</h1>
            </nav>
        </div>
    );
};

export default Header;